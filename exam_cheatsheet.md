# 🎯 考前 30 分钟速记清单

> **最后冲刺！记住这些核心考点，信心满满上考场！**

---

### 1. 核心语法

- **缩进**: 4个空格，是语法，不是美观。
- **注释**: `#` 单行, `"""多行"""`。
- **变量**: 无需声明类型，`a = 10`。
- **输入**: `name = input("提示:")` (永远返回字符串)。
- **输出**: `print(f"变量是 {name}")` (记住 f-string)。
- **类型转换**: `int()`, `float()`, `str()`, `list()`, `dict()`, `set()`, `tuple()`。

---

### 2. 字符串 `str` (不可变)

- **切片**: `s[start:stop:step]`
  - `s[:5]` (前5个)
  - `s[5:]` (第5个到结尾)
  - `s[-1]` (最后一个)
  - `s[::-1]` (反转)
- **常用方法**:
  - `s.strip()`: 去除两端空白。
  - `s.split(',')`: 按逗号分割成列表。
  - `','.join(my_list)`: 用逗号连接列表成字符串。
  - `s.find('a')`: 查找 'a' 的索引，找不到返回 -1。
  - `s.replace('a', 'b')`: 替换。
  - `s.lower()`, `s.upper()`: 大小写转换。

---

### 3. 列表 `list` (可变)

- **创建**: `my_list = [1, "a", 3.0]`
- **添加**:
  - `my_list.append(item)`: 在末尾添加。
  - `my_list.insert(index, item)`: 在指定位置插入。
- **删除**:
  - `my_list.remove(item)`: 删除第一个匹配的值。
  - `my_list.pop(index)`: 删除并返回指定索引的元素 (默认最后一个)。
  - `del my_list[index]`
- **访问**: `my_list[index]`, `my_list[-1]`。
- **排序**:
  - `my_list.sort()`: 原地排序。
  - `sorted(my_list)`: 返回新列表。
- **长度**: `len(my_list)`。
- **检查存在**: `item in my_list`。

---

### 4. 字典 `dict` (可变)

- **创建**: `my_dict = {"key1": "value1", "key2": "value2"}`
- **访问**:
  - `my_dict['key1']`: 键不存在会报错。
  - `my_dict.get('key1', "default")`: **推荐**，键不存在不报错。
- **增/改**: `my_dict['key1'] = "new_value"`。
- **删除**: `del my_dict['key1']` 或 `my_dict.pop('key1')`。
- **遍历**:
  - `for key in my_dict:` (遍历键)
  - `for value in my_dict.values():` (遍历值)
  - `for key, value in my_dict.items():` (**推荐**，遍历键值对)
- **获取**:
  - `my_dict.keys()`: 获取所有键。
  - `my_dict.values()`: 获取所有值。
  - `my_dict.items()`: 获取所有键值对。
- **检查存在**: `"key1" in my_dict`。

---

### 5. 流程控制

- **条件**: `if condition: ... elif condition: ... else: ...`
- **`for` 循环**:
  - `for item in my_list:` (遍历元素)
  - `for i in range(5):` (循环 0 到 4)
  - `for i, item in enumerate(my_list):` (同时获取索引和元素)
- **`while` 循环**: `while condition: ...`
- **循环控制**: `break` (跳出循环), `continue` (跳过本次)。

---

### 6. 函数 `def`

- **定义**: `def my_func(param1, param2="default"): ...`
- **返回**: `return value` 或 `return val1, val2` (返回元组)。
- **调用**: `my_func("hello")` 或 `my_func(param1="hello")`。

---

### 7. 文件操作 (必考)

- **标准模板**:
  ```python
  # 读取
  with open("filename.txt", "r") as f:
      for line in f:
          print(line.strip())

  # 写入 (覆盖)
  with open("filename.txt", "w") as f:
      f.write("Hello\n")

  # 追加
  with open("filename.txt", "a") as f:
      f.write("World\n")
  ```
- **模式**: `r` (读), `w` (写/覆盖), `a` (追加)。

---

### 8. Pythonic 写法 (加分项)

- **列表推导式**:
  - `squares = [x**2 for x in range(10)]`
  - `evens = [x for x in range(10) if x % 2 == 0]`
- **变量交换**: `a, b = b, a`
- **字符串反转**: `s[::-1]`
- **用 `in` 检查成员**: `if item in my_list:`
- **用 `enumerate` 获取索引**: `for i, item in enumerate(my_list):`

---

**最后提醒：**
- **仔细读题**：理解题目要求是成功的一半。
- **代码清晰**：使用有意义的变量名。
- **检查边界**：考虑空列表、空文件等特殊情况。

**祝你考试顺利！**